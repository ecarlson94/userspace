- shell:
    - apt update
    - apt upgrade -y
    - apt-get update
    - apt-get upgrade -y
    - [ apt install -y scdaemon pcscd ubuntu-dev-tools gnupg, Installing GNUPG... ]
    - [ apt install -y fontconfig, Installing font dependencies... ]
    - [ apt install -y, Installing docker... ]
    - [ groupadd -f docker, Adding Docker group... ]
    - [ usermod -aG docker $USER, Adding user to Docker group... ]
