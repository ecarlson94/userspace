# =============================================================================
#                                Key Bindings
# =============================================================================
bindkey -r "^h"
bindkey -r "^j"
bindkey -r "^k"
bindkey -r "^l"

# Disable "^D" (vi movements in tmux will exit terminal)
set -o ignoreeof

